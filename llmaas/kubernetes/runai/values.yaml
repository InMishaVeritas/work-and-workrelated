# Values for Run-AI Helm chart
global:
  platform: k8s
  clusterName: llmaas
  defaultStorageClass: standard

gpu-operator:
  enabled: true

runai-scheduler:
  enabled: true

runai-dashboard:
  enabled: true
  adminUsername: admin
  adminPassword: admin123  # Change this in production