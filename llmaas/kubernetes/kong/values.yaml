proxy:
  http:
    enabled: true       # Activer l'écoute HTTP
    port: 80
    hostPort: 80       # si hostPort est utilisé dans kind
  tls:
    enabled: false      # Désactiver HTTPS si vous ne voulez qu'HTTP
