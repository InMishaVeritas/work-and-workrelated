# kong-secret.yaml
apiVersion: v1
kind: Secret
metadata:
 name: cogip              # Must match name referenced in KongConsumer
 namespace: kserve-test
 labels:
   konghq.com/credential: key-auth
type: Opaque
stringData:
 kongCredType: key-auth
 key: cogip               # Your chosen API key value